// Compatibility command: v8 intentionally replaces the old per-hotel overrides
// with selected-name searches for every hotel. Run the current routing suite.
require('./test-widget-routing-v8.cjs');
